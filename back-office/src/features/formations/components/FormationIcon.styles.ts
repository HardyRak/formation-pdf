import type { CSSProperties } from 'react';

export const styles: { wrap: CSSProperties } = {
  wrap: {
    display: 'inline-flex',
    alignItems: 'center',
    justifyContent: 'center',
    lineHeight: 0,
  },
};
