import type { CSSProperties } from 'react';

export const styles: { card: CSSProperties } = {
  card: {
    background: 'var(--surface)',
    border: '1px solid var(--border)',
    borderRadius: 'var(--radius-md)',
    padding: 'var(--space-md)',
  },
};
