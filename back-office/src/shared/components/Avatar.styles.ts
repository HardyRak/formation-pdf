import type { CSSProperties } from 'react';

export const styles: { tile: CSSProperties } = {
  tile: {
    width: '36px',
    height: '36px',
    borderRadius: '12px',
    color: '#fff',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    fontWeight: 800,
    fontSize: '14px',
    flexShrink: 0,
  },
};
