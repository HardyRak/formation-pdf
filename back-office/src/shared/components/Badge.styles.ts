import type { CSSProperties } from 'react';

export const styles: { badge: CSSProperties } = {
  badge: {
    padding: '3px 10px',
    borderRadius: '999px',
    fontSize: '12px',
    fontWeight: 700,
  },
};
