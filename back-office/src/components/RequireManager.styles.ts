import type { CSSProperties } from 'react';

export const styles: { fullscreen: CSSProperties } = {
  fullscreen: {
    display: 'flex',
    minHeight: '100vh',
    alignItems: 'center',
    justifyContent: 'center',
  },
};
