import type { CSSProperties } from 'react';

export const styles: { box: CSSProperties; action: CSSProperties } = {
  box: { padding: '40px', textAlign: 'center', color: 'var(--text-faint)' },
  action: { marginTop: '12px' },
};
