# ============================================================
# PDF Formation — Back-office (Vite + React)
# Copier ce fichier en `.env` (jamais commité) et adapter.
# ============================================================

# URL de base de l'API NestJS (sans préfixe /v1 ; celui-ci est ajouté
# par le proxy Vite ou par le contrat du client).
# En dev, le proxy Vite redirige /v1 -> VITE_API_URL.
VITE_API_URL=http://localhost:3000
